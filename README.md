# Alpine Earthworks - React + Vite + Tailwind

This project is now set up as a React app powered by Vite with Tailwind CSS.

## Quick Start

1. Install dependencies:

   ```bash
   npm install
   ```

2. Start the development server:

   ```bash
   npm run dev
   ```

3. Build for production:

   ```bash
   npm run build
   ```